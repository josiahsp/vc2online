<page-content>

	<page-title>Editing Event</page-title>

	<%= render 'form', event: @event %>
	
	<%= button_to "Delete", {:controller => :events,
		:action => 'destroy', :id => @event.id }, :method => :delete, data: { confirm: "Are you sure you want to delete this event? This action cannot be undone."}, :class => "secondary-button" %>
	
</page-content>